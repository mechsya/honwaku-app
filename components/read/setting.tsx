import BottomSheet, { BottomSheetView } from "@gorhom/bottom-sheet";
import { Text } from "react-native";

export default function Setting() {
  return (
    <BottomSheet>
      <BottomSheetView>
        <Text>Helsddslo worldaa</Text>
      </BottomSheetView>
    </BottomSheet>
  );
}
